# 시크릿 — 실값 커밋 금지 (ADR-006 운영 1 / ADR-007 §7)
.env
.env.*
!.env.example

# 원본 데이터 제외. 산출물만 seed/ 로 커밋.
data/*
!data/README.md

# 파이썬 (ADR-014)
.venv/
__pycache__/
.pytest_cache/

# run_all.py 통합 실행 로그 — 실행마다 쌓이는 산출물이라 커밋하지 않는다
pipeline/logs/

# IDE
.idea/

# OS
.DS_Store
